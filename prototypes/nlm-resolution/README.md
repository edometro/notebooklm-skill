# nlm resolution prototype

Question: do the proposed project binding and user path binding resolution rules feel predictable when pushed through common directory layouts?

Run it with:

```bash
python3 prototypes/nlm-resolution/tui.py
```

This is throwaway code. It does not call NotebookLM and does not read or write real .notebooklmrc or workspace-map.json files.
